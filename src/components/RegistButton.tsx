import { Button, styled } from '@mui/material'

const RegistButton = styled(Button)({
	backgroundColor: '#30384e',
	color: '#fff',
	width: '150px',
	'&:hover': {
		backgroundColor: '#30384e95',
		color: '#000'
	}
})

export default RegistButton
